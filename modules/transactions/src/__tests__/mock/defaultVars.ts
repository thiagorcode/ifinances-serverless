import { APIGatewayProxyEvent, Context, Callback } from 'aws-lambda';

export const mockEvent = {} as APIGatewayProxyEvent;
export const mockContext = {} as Context;
export const mockCallback = {} as Callback;
